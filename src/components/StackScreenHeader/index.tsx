import {View, Text} from 'react-native';
import React from 'react';
import {Container, ScreenName} from './styles';

const StackScreenHeader = () => {
  return (
    <Container>
      <ScreenName>RECENT</ScreenName>
    </Container>
  );
};

export default StackScreenHeader;
